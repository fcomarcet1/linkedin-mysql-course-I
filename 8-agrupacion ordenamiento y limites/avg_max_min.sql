SELECT AVG(price) AS precio_medio FROM products;

SELECT MIN(price)AS precio_min FROM products;

SELECT MAX(price)AS precio_max FROM products;

# Obtener al valor minimo de una lista de valores.
SELECT LEAST(2,3,56,8,6) AS result;
